import { View, Text } from "react-native";
import React from "react";
import { styles } from "./style";

const Profile = () => {
  return (
    <View style={styles.card}>
      <Text style={styles.cardText}>This is Profile Screen</Text>
    </View>
  );
};

export default Profile;
